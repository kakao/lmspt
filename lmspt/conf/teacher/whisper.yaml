model_name: large-v3
download_root: ${machine.hf_home}/models/whisper
feature_extractor:
  _target_: lmspt.model_codec.teacher.whisper.WhisperFeatureExtractor
  model_name: ${..model_name}
  download_root: ${..download_root}
model:
  _target_: lmspt.model_codec.teacher.whisper.WhisperASRTeacher
  sample_rate: ${aux_decoder.sample_rate}
  distill_type: mse
  download_root: ${..download_root}
  model_name: ${..model_name}